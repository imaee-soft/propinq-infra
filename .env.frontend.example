# Frontend (contenedor nginx). Cargado con env_file en docker-compose.
# POIs en el mapa del home (GET /api/v1/pois/within)
# false = local / presentación sin import OSM
# true  = CT102 / producción
MAP_POIS_ENABLED=false
